import * as React from "react"
import { {{ capitalize compononentName }} } from '../src'

export default {
  title: "Components/{{ capitalize compononentName }}"
}

export const default = () => < {{ capitalize componentName }}>{{capitalize componentName}}</ {{capitalize componentName}}>
